import { startStimulusApp } from '@symfony/stimulus-bundle';

const app = startStimulusApp();
// security any custom, 3rd party controllers here
// app.security('some_controller_name', SomeImportedController);
